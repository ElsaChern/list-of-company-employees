import { HeaderSearchText, HeaderWrapper } from './styled';

const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderSearchText>Поиск</HeaderSearchText>
    </HeaderWrapper>
  );
};

export default Header;
