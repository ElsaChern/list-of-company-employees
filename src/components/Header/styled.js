import { styled } from 'styled-components';

const HeaderWrapper = styled.div`
  padding: 16px 24px;
`;

const HeaderSearchText = styled.p`
  margin: 0;
  font-weight: 700;
  font-size: 24px;
  line-height: 28px;
`;

export { HeaderWrapper, HeaderSearchText };
